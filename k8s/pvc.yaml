apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tritondft-workdir
  namespace: datahub-llm
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 100Gi
  storageClassName: rook-cephfs
